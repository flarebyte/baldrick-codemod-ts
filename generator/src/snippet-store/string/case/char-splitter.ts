/**
 * Split a string by ThisString
 * @param text the text to split
 * @returns an array of the strings that were separated by ThisString
 */
export const splitByThisString = (text: string): string[] =>
  text.split('separator');
